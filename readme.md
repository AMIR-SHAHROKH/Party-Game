# Party Game (FastAPI + Socket.IO + React)

Minimal multiplayer question & answer party game:
- FastAPI backend with Socket.IO (real-time)
- React (Vite) frontend
- Postgres DB
- Docker + docker-compose for local LAN deployment

## Quick start

1. Build & run:
```bash
docker-compose up --build
